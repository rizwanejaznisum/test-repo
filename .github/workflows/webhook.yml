name: webhook

on:
  repository_dispatch:
    types: do-something

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    
      - name: github pull
        uses: actions/checkout@v2
      - run: |
          git pull
          existed_in_remote=$(git ls-remote --heads origin ${{ github.event.client_payload.branch }})
          if [[ -z ${existed_in_remote} ]]; then
            echo 0
          else
            git checkout origin/${{ github.event.client_payload.branch }}
            git pull
          fi
    
      - name: Create Report
        run: |
          echo "${{ github.event.client_payload.text }}" > ./report2.html 
          
      - name: Commit files
        run: |
          git config --local user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          echo "check here"
          git add ./report2.html
          git commit -m "Add changes"
      - name: Push changes
        uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: ${{ github.event.client_payload.branch }}
          
           
